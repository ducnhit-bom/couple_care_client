# Phase 8: Code Generation & Build Runner

## Context
- Research report: [research-report.md](./research-report.md)
- Priority: P2 | Status: Pending

## Overview

Configure build_runner, create example freezed model and Riverpod annotated provider to verify code gen pipeline works.

## Implementation Steps

### 1. Build Runner Configuration

**File:** `/Users/baonguyen/Desktop/training/devops/cc/build.yaml` (create)

```yaml
targets:
  $default:
    builders:
      freezed:
        generate_for:
          include:
            - lib/**/*.dart
      json_serializable:
        generate_for:
          include:
            - lib/**/*.dart
        options:
          explicit_to_json: true
          field_rename: snake
```

### 2. Example Freezed Model (Auth User Entity)

**File:** `/Users/baonguyen/Desktop/training/devops/cc/lib/features/auth/domain/entities/user_entity.dart` (create)

```dart
import 'package:freezed_annotation/freezed_annotation.dart';

part 'user_entity.freezed.dart';
part 'user_entity.g.dart';

@freezed
class UserEntity with _$UserEntity {
  const factory UserEntity({
    required String id,
    required String email,
    String? displayName,
    String? photoUrl,
  }) = _UserEntity;

  factory UserEntity.fromJson(Map<String, dynamic> json) =>
      _$UserEntityFromJson(json);
}
```

### 3. Example Riverpod Annotated Provider

**File:** `/Users/baonguyen/Desktop/training/devops/cc/lib/features/auth/presentation/providers/auth_provider.dart` (create)

```dart
import 'package:riverpod_annotation/riverpod_annotation.dart';

part 'auth_provider.g.dart';

@riverpod
class AuthNotifier extends _$AuthNotifier {
  @override
  AsyncValue<void> build() {
    return const AsyncData(null);
  }

  // Placeholder methods - implement with actual auth logic later
  Future<void> signIn({
    required String email,
    required String password,
  }) async {
    state = const AsyncLoading();
    // TODO: implement sign in
    state = const AsyncData(null);
  }

  Future<void> signOut() async {
    state = const AsyncLoading();
    // TODO: implement sign out
    state = const AsyncData(null);
  }
}
```

### 4. Run Code Generation

```bash
cd /Users/baonguyen/Desktop/training/devops/cc
dart run build_runner build --delete-conflicting-outputs
```

This generates:
- `user_entity.freezed.dart`
- `user_entity.g.dart`
- `auth_provider.g.dart`

### 5. Add Build Runner Script to pubspec.yaml (optional convenience)

Add to scripts section or document in README:

```bash
# One-time build
dart run build_runner build --delete-conflicting-outputs

# Watch mode (during development)
dart run build_runner watch --delete-conflicting-outputs
```

## Related Code Files
| Action | File |
|--------|------|
| create | `build.yaml` |
| create | `lib/features/auth/domain/entities/user_entity.dart` |
| create | `lib/features/auth/presentation/providers/auth_provider.dart` |
| auto-generated | `lib/features/auth/domain/entities/user_entity.freezed.dart` |
| auto-generated | `lib/features/auth/domain/entities/user_entity.g.dart` |
| auto-generated | `lib/features/auth/presentation/providers/auth_provider.g.dart` |

All paths relative to `/Users/baonguyen/Desktop/training/devops/cc/`.

## Success Criteria
- `build_runner build` completes without errors
- Generated `.freezed.dart` and `.g.dart` files appear
- `UserEntity.fromJson()` / `.toJson()` work correctly
- `authNotifierProvider` auto-generated by riverpod_generator
- `flutter analyze` still passes (generated files excluded from lint)

## Risk Assessment
- build_runner version conflicts. Mitigation: versions in pubspec.yaml are compatible.
- Riverpod generator requires `riverpod_annotation` + `riverpod_generator` same major version. Confirmed compatible in Phase 1 deps.
